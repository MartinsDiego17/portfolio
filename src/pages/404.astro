---
import Layout404 from "../layouts/Layout404.astro";
---

<Layout404>
    <section>
        <img
            src="https://midu.dev/images/this-is-fine-404.gif"
            alt="gif page not-found"
        />
        <h1>404</h1>
        <h3>¡Ups! Página no encontrada</h3>
        <p>
            La página que buscas no existe o ha sido movida. No te preocupes,
            estos errores son normales en programación.
        </p>
        <a href="/" id="button-back-home" transition:animate="slide">
            <span
                ><svg
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke-width="1.5"
                    stroke="currentColor"
                    class="size-6"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M15.75 19.5 8.25 12l7.5-7.5"></path>
                </svg>
            </span>
            <span>Volver al inicio</span>
        </a>
    </section>
</Layout404>

<style>
    section {
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        line-height: 1;
        row-gap: 10px;
        padding-bottom: 10vh;
    }
    section img {
        filter: saturate(200%);
        width: 25vw;
    }
    h1 {
        font-size: 6rem;
        color: white;
        font-weight: bold;
    }
    h3 {
        color: #ccc;
        font-weight: bold;
        font-size: 2rem;
    }
    p {
        color: #aaa;
        width: 20vw;
        line-height: 1.7;
        text-align: center;
    }
    a {
        background-color: var(--normal);
        padding: 15px 30px;
        border-radius: 1em;
        color: var(--dark);
        font-weight: bold;
        display: flex;
        place-items: center;
        column-gap: 10px;
        cursor: pointer;
        box-shadow: 0px 10px 100px var(--normal);
        transition: 0.2s;
        &:hover {
            transform: translateY(10px);
            box-shadow: 0px 0px 0px transparent;
        }
    }
    p::before {
        content: "";
        position: absolute;
        width: 20%;
        top: 30%;
        left: 10%;
        height: 20%;
        border-radius: 50%;
        background: radial-gradient(
            circle at center,
            var(--creme),
            var(--normal) 100%
        );
        filter: blur(100px);
        z-index: -1;
        opacity: 0.4;
    }
    a span:first-child svg{
        width: 20px;
        height: 20px;
    }

    @media (max-width: 430px) {
        section h1 {
            font-size: 5em;
        }
        section img {
            width: 80%;
            margin-bottom: 5vh;
        }
        section h3{
            text-align: center;
        }

    }
    @media (max-width: 1024px) {
       section p {
        width: 80%;
        text-wrap: balance;
       } 
    }
</style>
